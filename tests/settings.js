import { MochaRunner, describe, it, before, after, beforeEach, afterEach, xdescribe, xit, specify, xspecify, context, xcontext } from "meteor/practicalmeteor:mocha"
import { expect } from "chai"
import { Settings } from "meteor/jsep:settings"

describe("Settings", ()=>{

  xit("should get private settings");
  xit("should get public settings");
  xit("should get settings from env var");
  xit("should add custom settings");
  xit("should read custom settings");
  xit("should throw error with required settings");


});